export interface EmailVerificationFormProps {
    setIsEmailVerified: React.Dispatch<React.SetStateAction<boolean | null>>;
};

export interface CheckIsEmailVerifiedProps {
    setIsEmailVerified:React.Dispatch<React.SetStateAction<boolean | null>>;
}